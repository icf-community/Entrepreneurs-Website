# Dev/test-only additions on top of requirements.txt. Never installed in the
# production image (Dockerfile installs requirements.txt only).
-r requirements.txt

iniconfig==2.3.0
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
